# Minimal dependencies for demo Lambda function
# Note: Only the essentials to keep the package size up but avoid circular imports

# Additional realistic dependencies for larger package size (no complex imports)
Pillow>=10.0.0
pandas>=2.0.0
numpy>=1.24.0

# HTTP utilities - basic and reliable
requests>=2.31.0
urllib3>=1.26.0,<2.0.0
certifi>=2023.11.17

# Development tools (needed for GitHub Actions)
ruff>=0.1.0
black>=22.0.0